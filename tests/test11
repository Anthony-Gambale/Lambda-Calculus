;; Should print "value of x"
;; Despite the environment being filled with other bindings
(let ((x) (value of x))
     (let ((y) (value of y))
          (let ((z) (value of z))
               (apply (x) (y)))))
